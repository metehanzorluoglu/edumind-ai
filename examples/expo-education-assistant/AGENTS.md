# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This app is deliberately pinned to Expo SDK 54 (not the newest available SDK)
to match the sibling root app in this monorepo and this machine's installed
Node version (SDK 57's React Native 0.86 requires Node >=22.13; SDK 54 does
not). Do not "helpfully" upgrade to a newer SDK without re-checking both of
those constraints first.
